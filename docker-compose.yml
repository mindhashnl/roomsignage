version: '3'
services:
  web:
    build: .
    env_file: mysign/.env
    ports:
    - "8000:8000"
    links:
    - db
  db:
    image: postgres:12
    env_file: mysign/.env
    ports:
      - "5432:5432"
volumes:
  logvolume01: {}